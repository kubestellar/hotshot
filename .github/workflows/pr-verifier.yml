name: PR Verifier

on:
  pull_request_target:
    types: [opened, edited, synchronize, reopened]

permissions:
  checks: write
  pull-requests: read
  packages: read

jobs:
  verify:
    uses: hivecommons/infra/.github/workflows/reusable-pr-verifier.yml@main
    secrets: inherit
