name: Stale Issues

on:
  schedule:
    - cron: '0 0 * * *'  # Daily at midnight UTC
  workflow_dispatch:

permissions:
  issues: write

jobs:
  stale:
    uses: hivecommons/infra/.github/workflows/reusable-stale.yml@main
    secrets: inherit
