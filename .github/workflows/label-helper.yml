name: Label Helper

on:
  issue_comment:
    types: [created]

permissions:
  contents: read
  issues: write
  pull-requests: write

jobs:
  label-helper:
    uses: hivecommons/infra/.github/workflows/reusable-label-helper.yml@main
    secrets: inherit
